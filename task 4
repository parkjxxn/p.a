// Натуральне число m називається рівним дільником числа n, якщо частка і залишок від ділення n на m дорівнюють одне одному. Для заданого натурального числа n знайти кількість його рівних дільників.  

// Вхідні дані:  Натуральне число n (1 < n < 150).

// Вихідні дані: Вивести одне шукане число.

// Для знаходження кількості рівних дільників числа n, ми можемо перебрати всі числа від 1 до n і перевірити, чи задовольняють вони умову рівних дільників

#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

  // Зчитуємо число n зі стандартного вводу за допомогою scanf()
  
  // Ініціалізуємо змінну count нулем, яка буде використовуватись для підрахунку кількості рівних дільників числа n
  
    int count = 0;
    for (int i = 1; i <= n; i++) {
        if (n % i == n / i) {
            count++;
        }
    }

  // Виводимо значення count за допомогою printf().

    printf("%d\n", count);
    return 0;
}
